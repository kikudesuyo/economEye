import { JAN_CODE_DIGIT } from "./constant";

export class JanCode {
  constructor(id: string) {
    // if (this.isValidDigit(id)) {
    //   this.id = id;
    // }
  }
  // protected isValidDigit(id: string) {
  //   return id.length === JAN_CODE_DIGIT;
  // }
}
